.group
  .group__title 
    グループ作成
  .group__box
    = form_with model: @group, local: true do |f|
      .group__font
        = f.label :グループ名
        = f.text_field :name, class: "group__field"
        .group__font
        = f.label :メンバー追加
        .group__collection
          %input#search__field.group__field{type: "text"}
        .search__add
        .group__font グループメンバー
        .search__delete
          %p.member__current= current_user.name
          %input{name: "group[user_ids][]", type: "hidden", value: current_user.id}
        = f.submit "作成", class: "group__send"
    = link_to "トップページに戻る", :back, class: "edit__back"